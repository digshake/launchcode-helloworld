var max;
if(x > y) {
	max = x;
	console.log("x is bigger");
} else {
	max = y;
	if (x == y) {
		console.log("They are the same");
	} else {
		console.log("y is bigger");
	}
}
console.log("max is " + max);

